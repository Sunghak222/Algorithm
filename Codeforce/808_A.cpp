#include <bits/stdc++.h>
 
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
	cin.tie(nullptr);
    int t,n;
    cin >> t;
    for (int i = 0; i < t; i++)
    {
        cin >> n;
        vector<int> a;
        int temp;
        for (int j = 0; j < n; j++)
        {
            cin >> temp;
            a.push_back(temp);
        }
    }
}